name: xxsmartsystems
description: IoT Control Platform
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.12.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  mqtt_client: ^10.0.0
  flutter_tts: ^4.2.5
  flutter_staggered_grid_view: ^0.7.0
  flutter_svg: ^2.0.7
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  permission_handler: ^11.0.0
  connectivity_plus: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  web: 0.3.0